global int interrupt_count=0, sum = 0; 

int main(){
	int rst, last_addr, curr_addr; 
	
	while (!rst){
		if (last_addr == curr_addr){
			last_addr = curr_addrl 
			interrupt(curr_addr); 
		}
	}
	
return 0; 
}

void interrupt(int data){
	int sum_div; 
	sum = sum(audio_data); 
	interrupt_count++; 
	if (interrupt_count == 256) {
		sum_div = sum / 256; //8355840
		LED = sum_div; 
		interrupt_count = 0; 
	}
}
/*


*/